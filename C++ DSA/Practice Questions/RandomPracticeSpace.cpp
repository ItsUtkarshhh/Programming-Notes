#include<iostream>
#include<map>
#include<vector>
using namespace std;

vector<int> intersectionOfArrays(vector<int> arr1, vector<int> arr2) {
    if(arr1.size() == 0 ) return {};
}

int main() {
    int size;
    cin>>size;

    vector<int> arr(size);
    for(int i = 0; i < size; i++) {
        cin>>arr[i];
    }
}